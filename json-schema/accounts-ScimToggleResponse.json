{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScimToggleResponse",
  "description": "ScimToggleResponse schema from Docupilot accounts API",
  "type": "object",
  "properties": {
    "provisioning_mode": {
      "type": "string"
    },
    "scim_config": {
      "$ref": "#/components/schemas/ScimConfig"
    }
  },
  "required": [
    "provisioning_mode",
    "scim_config"
  ]
}